<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.dvlang</groupId>
  <artifactId>dv-framework</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>DV Framework</name>
  <description>
    Stateless, server-driven full-stack framework where one .dv file declares domain,
    database access and UI, and the runtime serves a working SPA with no manual frontend
    build. See docs/DV-Coder-Brief-v1.pdf for the full specification.
  </description>

  <modules>
    <module>dv-compiler</module>
    <module>dv-emitter-react</module>
    <module>dv-emitter-lit</module>
    <module>dv-lsp</module>
    <module>dv-runtime</module>
    <module>dv-cli</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!--
      Brief section 2 specifies Java 25. This environment provides only a Java 21 JDK,
      so the reactor is pinned to 21 to keep the build reproducible here. Bump
      maven.compiler.release to 25 (and re-pin plugin/dependency versions as needed)
      once a Java 25 toolchain is available.
    -->
    <maven.compiler.release>21</maven.compiler.release>

    <antlr4.version>4.13.2</antlr4.version>
    <junit.version>5.11.4</junit.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.antlr</groupId>
        <artifactId>antlr4-runtime</artifactId>
        <version>${antlr4.version}</version>
      </dependency>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
        </plugin>
        <plugin>
          <groupId>org.antlr</groupId>
          <artifactId>antlr4-maven-plugin</artifactId>
          <version>${antlr4.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
